# 仅用于控制是否运行真实 API 的集成测试
# API Key 必须在应用内设置（用户目录配置），不通过环境变量配置
RUN_INTEGRATION_TESTS=false

# 集成测试时指定只测某方：zhipu（智谱）| dashscope（通义），不设则用应用配置的 provider
# TEST_API_PROVIDER=zhipu
# TEST_API_PROVIDER=dashscope

# LangSmith 追踪（可选）：填写后自动开启 LLM/Agent 调用追踪
# 获取 API Key: https://smith.langchain.com/
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=
# LANGCHAIN_PROJECT=deepagentui

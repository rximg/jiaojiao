# jiaojiao-gateway 自身的 Secret Key（客户端鉴权，与 ai_models.json.jiaojiao.defaultApiKey 一致）
JIAOJIAO_SK=jjtk-20260306-default

# DashScope 上游 API Key（必填）
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# T2I 路由后端（z-image-turbo | dashscope），默认走本地 z-image-turbo
T2I_BACKEND=z-image-turbo

# GPU 模型权重挂载路径（使用 --profile gpu 时必填，宿主机路径）
MODEL_PATH=/mnt/models/z-image-turbo

# 返回给客户端的图片公网地址前缀（默认走网关代理，避免返回 Docker 内网域名）
Z_IMAGE_PUBLIC_URL=http://localhost:9021/z-image-turbo

# 集成测试轮询超时设置（可选）
# GATEWAY_POLL_TIMEOUT=300
# GATEWAY_POLL_INTERVAL=5
